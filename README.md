# auditorydecoding

This repository contains the code for the auditory decoding project with Mila and Neurosoft.

## Setup

Install the project dependencies (including development dependencies):

```bash
uv sync --all-extras
```

Install the pre-commit hooks:

```bash
uv run pre-commit install
```